<?xml version="1.0" encoding="utf-8"?>
<!--
  Widget list item: grouped routines row.
  Shows all routines compactly on one or two lines, joined with " · ".
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/routine_item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="start"
    android:paddingTop="4dp"
    android:paddingBottom="4dp">

    <!-- Subtle left accent (neutral gray) -->
    <TextView
        android:id="@+id/routine_accent"
        android:layout_width="3dp"
        android:layout_height="22dp"
        android:background="@color/widget_divider"
        android:layout_marginEnd="7dp"
        android:layout_marginTop="2dp"
        android:text="" />

    <!-- Routine icon placeholder -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="↺ "
        android:textColor="@color/widget_text_secondary"
        android:textSize="11sp"
        android:layout_marginTop="1dp" />

    <!-- All routine names, joined with " · ", wrapped to 2 lines -->
    <TextView
        android:id="@+id/tv_routine_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="Routine"
        android:textColor="@color/widget_text_primary"
        android:textSize="11sp"
        android:maxLines="2"
        android:ellipsize="end" />

</LinearLayout>
