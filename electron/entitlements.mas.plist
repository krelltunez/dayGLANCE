<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Allows the V8 JIT. Kept for parity with Electron's defaults; it is a
         no-op under MAS (Hardened Runtime is off — the App Sandbox governs
         execution instead). com.apple.security.cs.allow-unsigned-executable-memory
         is deliberately NOT included: it is a Hardened-Runtime concept that draws
         App Store review scrutiny and is unnecessary for the sandboxed build. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- MAS requires full App Sandbox -->
    <key>com.apple.security.app-sandbox</key>
    <true/>
    <!-- Outbound network (CalDAV, AI APIs, RevenueCat REST) -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- Inbound: the local WebSocket server (ws://127.0.0.1:7892) used by the
         Stream Deck plugin needs a listening socket, which the sandbox gates
         behind the network.server entitlement. -->
    <key>com.apple.security.network.server</key>
    <true/>
    <!-- NOTE: StoreKit in-app purchases need NO sandbox entitlement on macOS.
         com.apple.security.in-app-payments is the Apple Pay entitlement (not
         StoreKit) and is rejected by App Store validation on macOS, so it is
         intentionally omitted. IAP works via the sandbox + store signature alone. -->
    <!-- Read access to the system Calendar via the EventKit helper -->
    <key>com.apple.security.personal-information.calendars</key>
    <true/>
    <!-- Obsidian vault: user picks folder via open panel -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <!-- iCloud CloudDocuments sync -->
    <key>com.apple.developer.icloud-container-identifiers</key>
    <array>
      <string>iCloud.com.dayglance</string>
    </array>
    <key>com.apple.developer.icloud-services</key>
    <array>
      <string>CloudDocuments</string>
    </array>
    <!-- NOTE: no com.apple.developer.ubiquity-kvstore-identifier here. The desktop
         app's iCloud sync is entirely file-based (CloudDocuments); it does not use
         NSUbiquitousKeyValueStore. The entitlement also embedded the literal
         $(TeamIdentifierPrefix) macro (electron-builder, unlike Xcode, does not
         expand it), which failed App Store validation against the profile. -->
  </dict>
</plist>
